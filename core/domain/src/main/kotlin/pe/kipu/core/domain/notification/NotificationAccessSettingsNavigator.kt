package pe.kipu.core.domain.notification

/**
 * Opens the system screen where the user can grant notification listener access.
 */
fun interface NotificationAccessSettingsNavigator {
    fun openListenerSettings()
}
